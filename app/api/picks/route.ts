import {NextResponse} from 'next/server';import {serverClient} from '../../../lib/server';import {validPicks} from '../../../lib/game';
export async function POST(req:Request){const db=await serverClient();const {data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({error:'Log in first'},{status:401});try{const {week,picks}=await req.json();if(typeof week!=='string'||!/^\d{4}-\d{2}-\d{2}$/.test(week)||!Array.isArray(picks))throw Error('Invalid request');const {data:cats,error:ce}=await db.from('categories').select('id').eq('active',true);if(ce)throw ce;if(!validPicks(picks,(cats||[]).map(c=>c.id)))throw Error('Allocate exactly 100 points, maximum 25 per category');const {error}=await db.rpc('submit_lineup',{p_week:week,p_picks:picks});if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400})}}
