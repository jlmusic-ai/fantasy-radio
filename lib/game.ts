export const ZONE='America/New_York';
export function weekStart(date:Date){const parts=new Intl.DateTimeFormat('en-US',{timeZone:ZONE,year:'numeric',month:'2-digit',day:'2-digit',weekday:'short'}).formatToParts(date);const get=(k:string)=>parts.find(p=>p.type===k)!.value;const day=Number(get('day'));const weekday=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].indexOf(get('weekday'));const utc=new Date(Date.UTC(Number(get('year')),Number(get('month'))-1,day));utc.setUTCDate(utc.getUTCDate()-(weekday+6)%7);return utc.toISOString().slice(0,10)}
export function seasonStart(week:string){const d=new Date(week+'T12:00:00Z');const month=Math.floor(d.getUTCMonth()/3)*3;return `${d.getUTCFullYear()}-${String(month+1).padStart(2,'0')}-01`}
export function validPicks(picks:{category_id:string;points:number}[],ids:string[]){return picks.length===ids.length&&new Set(picks.map(p=>p.category_id)).size===ids.length&&picks.every(p=>ids.includes(p.category_id)&&Number.isInteger(p.points)&&p.points>=0&&p.points<=25)&&picks.reduce((n,p)=>n+p.points,0)===100}
